import 'package:flutter/material.dart';

class DashboardItemProperties {
  String title;
  Widget icon;
  String route;
  Color color;
  DashboardItemProperties(this.title, this.icon, this.route, this.color);
}
